# CLAUDE.md

This project keeps its agent and contributor working notes in
**[`AGENTS.md`](AGENTS.md)** (the [agents.md](https://agents.md) convention).
Claude Code reads this file, so:

**See [`AGENTS.md`](AGENTS.md) for everything**: the confidence/provenance rule,
the repo layout, the build gate (`npm run build` before every commit), the
attribution model, the multi-technique + independent-reconstruction methodology,
the wasm-analysis (warden) operational guide, and the reference-implementation
conventions.

Quick reminders: stage explicit paths (never `git add -A`: concurrent editors);
commit but do not push; no real PII/keys; `confirmed` needs two independent
techniques.
